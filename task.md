# Tarefas de Implementação (Aguardando Atendimento)

- [ ] `data_processor.py`
  - [ ] Substituir leitura de `SOLICITAÇÃO` por `AGUARDANDO ATENDIMENTO`.
  - [ ] Criar `apenas_aberta` e `apenas_aguardando`.
  - [ ] Atualizar `aberta_ou_solicitacao` para somar as duas (para o 'Ontem p/ Hoje').
  - [ ] Ajustar `infra_sla_vencido` e `sistemas_sla_vencido` para usar apenas `apenas_aberta`.
  - [ ] Criar `infra_sla_vencido_aguardando` e `sistemas_sla_vencido_aguardando`.
  - [ ] Criar `total_sla_vencido_aguardando`.
  - [ ] Retornar os novos campos no dicionário.
- [ ] `main.py`
  - [ ] Adicionar nova linha de colunas em BLOCO 2 para abrigar as 2 novas métricas ao lado/antes do SLA > 3 Dias.
  - [ ] Adicionar o `Total SLA Vencido (Aguardando Atendimento)` no bloco oculto de rodapé (ao lado do Total SLA Abertas).
- [ ] `report_generator.py`
  - [ ] Adicionar as duas novas linhas de "AGUARDANDO ATENDIMENTO" na impressão em PDF.
- [ ] `history_manager.py` & `historico_huddle.csv`
  - [ ] Adicionar `Aguardando Atendimento` na lista `COLUMNS`.
  - [ ] Obter a quantidade total de "Aguardando Atendimento" e passar no `save_history`.
  - [ ] Reformatar o cabeçalho do `historico_huddle.csv` atual para incluir a nova coluna.
  - [ ] Preencher o valor '0' nas linhas de datas passadas no `.csv`.
- [ ] Validar e gerar `walkthrough.md`.
